namespace Sms.Domain.Common
{
    /// <summary>Shared by Students (doc/Modules/10) and, later, Employees (Module 12).</summary>
    public enum Gender : short
    {
        Male = 1,
        Female = 2,
    }
}
