@using System.Globalization
@inject Sms.Web.Services.SchoolBrandMark BrandMark
@{
    var culture = CultureInfo.CurrentUICulture;
    var isRtl = culture.TextInfo.IsRightToLeft;
    var lang = culture.TwoLetterISOLanguageName;
    var bootstrapCss = isRtl ? "~/lib/bootstrap/css/bootstrap.rtl.min.css" : "~/lib/bootstrap/css/bootstrap.min.css";
    var returnUrl = Context.Request.Path + Context.Request.QueryString;
    var version = typeof(Sms.Web.Program).Assembly.GetName().Version?.ToString(3) ?? "1.0.0";

    // BR-SCH-006: the gate wears the same mark as every screen behind it — a school recognises its
    // own sign-in page by its logo long before it reads the field labels. Until one is uploaded it
    // wears the product's generic mark, exactly as the sidebar does. Deliberately not a school's
    // logo shipped as a file in wwwroot: this product is one deployment per school, so a file
    // committed here is worn by every other school's sign-in screen too, and the slot that answers
    // this properly already exists on the school profile.
    var logoVersion = await BrandMark.VersionAsync();
}
<!DOCTYPE html>
<html lang="@lang" dir="@(isRtl ? "rtl" : "ltr")">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SMS</title>
    <link rel="stylesheet" href="@bootstrapCss" asp-append-version="true" />
    <link rel="stylesheet" href="~/lib/bootstrap-icons/bootstrap-icons.min.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/lib/fonts/fonts.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="sms-auth">
    <div class="sms-auth__bg" aria-hidden="true"></div>
    <div class="sms-auth__wrap">
        <div class="sms-auth__top">
            <form asp-controller="Home" asp-action="SetLanguage" method="post" class="d-inline">
                <input type="hidden" name="returnUrl" value="@returnUrl" />
                <input type="hidden" name="culture" value="@(isRtl ? "en" : "ar")" />
                <button type="submit" class="btn btn-sm sms-auth__lang"><i class="bi bi-translate"></i> @(isRtl ? "English" : "العربية")</button>
            </form>
        </div>
        <main class="sms-auth__card">
            <div class="sms-auth__brand">
                @* Decorative: the product name is the next line, and nothing is gained by hearing
                   the file described after it. *@
                <span class="sms-auth__mark@(logoVersion.HasValue ? " sms-auth__mark--logo" : "")">
                    @if (logoVersion is int uploaded)
                    {
                        <img src="@Url.Action("BrandLogo", "Home", new { v = uploaded })" alt="" class="sms-auth__logo" />
                    }
                    else
                    {
                        <i class="bi bi-mortarboard-fill" aria-hidden="true"></i>
                    }
                </span>
                <h1 class="sms-auth__name">SMS</h1>
                <p class="sms-auth__sub">@(isRtl ? "نظام إدارة المدارس" : "School Management System")</p>
            </div>
            @RenderBody()
        </main>
        <footer class="sms-auth__foot">
            <span>&copy; 2026 SMS &middot; @(isRtl ? "جميع الحقوق محفوظة" : "All rights reserved")</span>
            <span class="sms-auth__version">v@(version)</span>
        </footer>
    </div>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script>
        document.querySelectorAll('[data-password-toggle]').forEach(function (btn) {
            btn.addEventListener('click', function () {
                var input = document.getElementById(btn.getAttribute('data-password-toggle'));
                var show = input.type === 'password';
                input.type = show ? 'text' : 'password';
                btn.querySelector('i').className = show ? 'bi bi-eye-slash' : 'bi bi-eye';
            });
        });
    </script>
    @RenderSection("Scripts", required: false)
</body>
</html>
